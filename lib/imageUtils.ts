// Utilidad para obtener imagen procesada
export const getProcessedImage = async (originalUrl: string): Promise<string> => {
  // Simplemente retorna la URL original ya que no procesamos imágenes
  return originalUrl;
};